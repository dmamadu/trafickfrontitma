import { FormulaireIdentificationComponent } from './formulaire-identification.component'

describe('FormulaireIdentificationComponent', () => {
  it('should mount', () => {
    cy.mount(FormulaireIdentificationComponent)
  })
})